<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aaa.politindex">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main_screen.MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".authentication.AuthActivity" />
        <activity android:name=".authentication.AuthPhoneActivity">
            android:windowSoftInputMode="adjustResize"
        </activity>
        <activity android:name=".authentication.SendSmsActivity">
            android:windowSoftInputMode="adjustResize"
        </activity>
        <activity android:name=".main_screen_for_auth_user.MainAuthUserActivity" />
        <activity android:name=".search_by_figures.SearchByFiguresActivity" />
        <activity android:name=".figure_main_screen.FigureMainActivity" />
        <activity android:name=".user_profile.UserProfileActivity" />
        <activity android:name=".date_information_activity.DateInfoActivity" />
        <activity android:name=".time_line_info_activity.TimeLineActivity"></activity>
    </application>

</manifest>